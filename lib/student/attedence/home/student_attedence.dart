import 'package:flutter/material.dart';

class StudentAttedenceScreen extends StatelessWidget {
  const StudentAttedenceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Student Attedence"),
      ),
    );
  }
}
